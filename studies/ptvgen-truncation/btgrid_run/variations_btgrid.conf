# =============================================================================
# Variations for --bt-grid mode (scetlib-run-qT.py --bt-grid).
#
# Central only. The cached bT-grid is purely perturbative; NP variations
# (lambda_inf, lambda2, ..., lambda_inf_nu, lambda2_nu, ...) are factorized
# out of the grid by construction and should be applied in the Python fit
# at reconstruction time, not via this file.
#
# If you later want resummation-scale theory uncertainties on the *grid*
# (kappaFO, lambda, transition_points, muB/nuB/muS/nuS variations), copy
# the relevant entries from prod/scetlib_run/examples/variations/variations_res.conf
# below -- those are all NP-free and safe to use here.
# =============================================================================

[0]
# central result
